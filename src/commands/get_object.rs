/* use PebbleVault;

pub fn main() {
    let args: Vec<String> = std::env::args().collect();
    
    let db: usize = args[1].parse().unwrap();
    let key: &str = &args[2];
    
    PebbleVault::get_object(db, key);
}*/